@tool
extends EditorPlugin
## This package is a RUNTIME LIBRARY, not an editor tool. There is nothing to
## register: the classes are global (`class_name`) and the profiles are plain
## Resources. The plugin entry exists only so the package appears in the
## Project > Plugins list next to the Sky3D install it depends on.
